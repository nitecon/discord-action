{
  "name": "discord-action",
  "version": "1.0.0",
  "description": "GitHub Action to send detailed event information to Discord via webhook",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --license licenses.txt",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nitecon/discord-action.git"
  },
  "keywords": [
    "github-action",
    "discord",
    "webhook",
    "notification"
  ],
  "author": "Nitecon",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
